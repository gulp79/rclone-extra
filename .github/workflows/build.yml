# Github Actions build for rclone (Custom & Clean)
# Features:
# - Windows: CGO enabled (cmount), Static Linking (no DLL deps), Custom Icon
# - Linux:   CGO disabled (Pure Go), Static Linking, Multi-arch
# - Android: All architectures (Pure Go, no CGO)
# - On tag push: builds everything, uploads assets to the release, submits to WinGet

name: Build rclone-extra - Multi Platform

on:
  workflow_dispatch:
    inputs:
      target:
        description: "Seleziona cosa compilare"
        type: choice
        required: true
        default: all
        options:
          - all
          - linux
          - windows
          - android
  push:
    tags:
      - 'v*'

permissions:
  contents: write  # FIX: era 'read' — serve write per caricare gli asset sulla release

jobs:

  # --- JOB: DESKTOP & SERVER (Linux x64, Linux ARM64, Windows x64) ---
  build:
    name: Build ${{ matrix.name }}
    runs-on: ubuntu-latest
    timeout-minutes: 45

    # FIX: aggiunto 'github.event_name == 'push'' per gestire il trigger da tag
    # (su push da tag, inputs.target non esiste ed è vuoto → tutte le condizioni == false senza questo fix)
    if: |
      github.event_name == 'push' ||
      inputs.target == 'all'      ||
      inputs.target == 'linux'    ||
      inputs.target == 'windows'

    strategy:
      fail-fast: false
      matrix:
        include:
<<<<<<< HEAD
          - name: linux-amd64
            goos: linux
            goarch: amd64
            ext: ''
          - name: linux-arm64
            goos: linux
            goarch: arm64
            ext: ''
          - name: linux-arm-v7
            goos: linux
            goarch: arm
            goarm: 7
            ext: ''
          - name: windows-amd64
            goos: windows
            goarch: amd64
            ext: '.exe'
=======
          - job_name: linux
            os: ubuntu-latest
<<<<<<< HEAD
            go: '~1.26.3'
=======
            go: '~1.26.5'
>>>>>>> v1.74.4
            gotags: cmount
            build_flags: '-include "^linux/"'
            check: true
            quicktest: true
            racequicktest: true
            librclonetest: true
            deploy: true

          - job_name: linux_386
            os: ubuntu-latest
<<<<<<< HEAD
            go: '~1.26.3'
=======
            go: '~1.26.5'
>>>>>>> v1.74.4
            goarch: 386
            gotags: cmount
            quicktest: true

          - job_name: mac_amd64
            os: macos-latest
<<<<<<< HEAD
            go: '~1.26.3'
=======
            go: '~1.26.5'
>>>>>>> v1.74.4
            gotags: 'cmount'
            build_flags: '-include "^darwin/amd64" -cgo'
            quicktest: true
            racequicktest: true
            deploy: true

          - job_name: mac_arm64
            os: macos-latest
<<<<<<< HEAD
            go: '~1.26.3'
=======
            go: '~1.26.5'
>>>>>>> v1.74.4
            gotags: 'cmount'
            build_flags: '-include "^darwin/arm64" -cgo -macos-arch arm64 -cgo-cflags=-I/usr/local/include -cgo-ldflags=-L/usr/local/lib'
            deploy: true

          - job_name: windows
            os: windows-latest
<<<<<<< HEAD
            go: '~1.26.3'
=======
            go: '~1.26.5'
>>>>>>> v1.74.4
            gotags: cmount
            cgo: '0'
            build_flags: '-include "^windows/"'
            build_args: '-buildmode exe'
            quicktest: true
            deploy: true

          - job_name: other_os
            os: ubuntu-latest
<<<<<<< HEAD
            go: '~1.26.3'
=======
            go: '~1.26.5'
>>>>>>> v1.74.4
            build_flags: '-exclude "^(windows/|darwin/|linux/)"'
            compile_all: true
            deploy: true

          - job_name: go1.25
            os: ubuntu-latest
            go: '~1.25.12'
            quicktest: true
            racequicktest: true

    name: ${{ matrix.job_name }}

    runs-on: ${{ matrix.os }}
>>>>>>> 7dcbc8b4f3d89818642a22977d4307f836eafb6c

    steps:
<<<<<<< HEAD
      - name: Skip non matching matrix rows
        id: gate
        run: |
          TARGET="${{ inputs.target }}"
          GOOS="${{ matrix.goos }}"
          # Se il trigger è un tag push (TARGET vuoto) → non saltare nulla
          if [ -z "$TARGET" ]; then
            echo "skip=false" >> $GITHUB_OUTPUT
          elif [ "$TARGET" = "linux" ] && [ "$GOOS" != "linux" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
          elif [ "$TARGET" = "windows" ] && [ "$GOOS" != "windows" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi
=======
      - name: Get runner parameters
        id: get-runner-parameters
        run: |
          echo "year-week=$(date -u "+%Y%V")" >> $GITHUB_OUTPUT
          echo "runner-os-version=$ImageOS" >> $GITHUB_OUTPUT
>>>>>>> v1.74.4

      - name: Checkout
        if: steps.gate.outputs.skip == 'false'
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Go
<<<<<<< HEAD
        if: steps.gate.outputs.skip == 'false'
=======
        id: setup-go
>>>>>>> v1.74.4
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.x'
          check-latest: true
<<<<<<< HEAD
          cache: true
=======
          # Caching is handled explicitly below. setup-go's built-in cache
          # keys only on go.sum, so all matrix jobs collide on one key and
          # only the first to finish saves - which loses the cross-compile
          # build cache.
          cache: false

      - name: Get Go paths
        id: go-paths
        run: |
          echo "build-cache=$(go env GOCACHE)" >> $GITHUB_OUTPUT
          echo "mod-cache=$(go env GOMODCACHE)" >> $GITHUB_OUTPUT

      # The module cache holds downloaded source and is identical across
      # jobs (it depends only on go.sum, not on OS, arch or Go version), so
      # it is shared via a single key to avoid storing the same ~600 MiB in
      # every job's cache and overflowing the repo cache limit.
      - name: Module cache
        uses: actions/cache@v5
        with:
          path: ${{ steps.go-paths.outputs.mod-cache }}
          key: go-mod-${{ steps.get-runner-parameters.outputs.year-week }}-${{ hashFiles('go.sum') }}
          restore-keys: |
            go-mod-${{ steps.get-runner-parameters.outputs.year-week }}-
            go-mod-

      # The build cache holds compiled artifacts which are specific to the
      # OS, arch and Go version, so it is kept per job.
      - name: Build cache
        uses: actions/cache@v5
        with:
          path: ${{ steps.go-paths.outputs.build-cache }}
          key: go-build-${{ matrix.job_name }}-${{ steps.get-runner-parameters.outputs.runner-os-version }}-go${{ steps.setup-go.outputs.go-version }}-${{ steps.get-runner-parameters.outputs.year-week }}-${{ hashFiles('go.sum') }}
          restore-keys: |
            go-build-${{ matrix.job_name }}-${{ steps.get-runner-parameters.outputs.runner-os-version }}-go${{ steps.setup-go.outputs.go-version }}-${{ steps.get-runner-parameters.outputs.year-week }}-
            go-build-${{ matrix.job_name }}-${{ steps.get-runner-parameters.outputs.runner-os-version }}-go${{ steps.setup-go.outputs.go-version }}-
>>>>>>> v1.74.4

      # --- WINDOWS PRE-REQUISITES ---
      - name: Install MinGW (Windows Only)
        if: steps.gate.outputs.skip == 'false' && matrix.goos == 'windows'
        run: |
          sudo apt-get update
          sudo apt-get install -y mingw-w64

      - name: Setup WinFsp Headers (Windows Only)
        if: steps.gate.outputs.skip == 'false' && matrix.goos == 'windows'
        run: |
          wget -q -O winfsp.zip https://github.com/winfsp/winfsp/archive/refs/tags/v2.1.zip
          unzip -q winfsp.zip
          echo "WINFSP_INC=$(pwd)/winfsp-2.1/inc/fuse" >> $GITHUB_ENV

      - name: Setup Icon (Windows Only)
        if: steps.gate.outputs.skip == 'false' && matrix.goos == 'windows'
        run: |
          go install github.com/akavel/rsrc@latest
          curl -L https://raw.githubusercontent.com/rclone/rclone/master/graphics/logo/ico/logo_symbol_color.ico -o rclone.ico
          rsrc -ico rclone.ico -o rclone.syso

      # --- BUILD STEP ---
      - name: Build Rclone (${{ matrix.name }})
        if: steps.gate.outputs.skip == 'false'
        shell: bash
        run: |
          export GOOS=${{ matrix.goos }}
          export GOARCH=${{ matrix.goarch }}
          # Usa il tag git come versione se disponibile, altrimenti fallback
          VERSION="${{ github.ref_name }}"
          if [ -z "$VERSION" ]; then VERSION="v1.73.2-extra"; fi

          LDFLAGS_BASE="-s -w -buildid= -X github.com/rclone/rclone/fs.Version=${VERSION}"

          if [ "$GOOS" == "windows" ]; then
            echo "::group::Configuring Windows STATIC Build"
            export CGO_ENABLED=1
            export CC=x86_64-w64-mingw32-gcc
            export CXX=x86_64-w64-mingw32-g++
            export CGO_CFLAGS="-I$WINFSP_INC"
            export CGO_LDFLAGS="-static"
            LDFLAGS="$LDFLAGS_BASE -extldflags '-static -static-libgcc -static-libstdc++'"
            TAGS="-tags cmount"
            echo "::endgroup::"
          else
            echo "::group::Configuring Linux Pure-Static Build"
            export CGO_ENABLED=0
            TAGS="-tags netgo,osusergo"
            LDFLAGS="$LDFLAGS_BASE"
            echo "::endgroup::"
          fi

          echo "Building for $GOOS/$GOARCH..."
          go build -v -trimpath -mod=readonly -ldflags "$LDFLAGS" $TAGS -o rclone${{ matrix.ext }} .

      # FIX: crea lo zip e lo carica direttamente sulla release (solo su tag push)
      # Questo è l'asset che winget-releaser cerca con la regex '-windows-\w+\.zip$'
      - name: Package zip for Release (tag push only)
        if: steps.gate.outputs.skip == 'false' && startsWith(github.ref, 'refs/tags/')
        run: zip rclone-${{ matrix.name }}.zip rclone${{ matrix.ext }}

      - name: Upload zip to GitHub Release (tag push only)
        if: steps.gate.outputs.skip == 'false' && startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload ${{ github.ref_name }} rclone-${{ matrix.name }}.zip --clobber

<<<<<<< HEAD
      - name: Upload Artifact (per uso interno tra job)
        if: steps.gate.outputs.skip == 'false'
        uses: actions/upload-artifact@v7
=======
      - name: Trim module cache before saving
        if: always()
        run: |
          # The downloaded module .zip archives are not needed to build from
          # the already-extracted module cache, so drop them before the cache
          # is saved - this roughly halves the module cache size.
          modcache=$(go env GOMODCACHE | tr '\\' '/')
          find "$modcache/cache/download" -name '*.zip' -type f -delete 2>/dev/null || true

  lint:
    if: inputs.manual || (github.repository == 'rclone/rclone' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name != github.event.pull_request.base.repo.full_name))
    timeout-minutes: 30
    name: "lint"
    runs-on: ubuntu-latest

    steps:
      - name: Get runner parameters
        id: get-runner-parameters
        run: |
          echo "year-week=$(/bin/date -u "+%Y%V")" >> $GITHUB_OUTPUT
          echo "runner-os-version=$ImageOS" >> $GITHUB_OUTPUT

      - name: Checkout
        uses: actions/checkout@v6
>>>>>>> v1.74.4
        with:
          name: rclone-${{ matrix.name }}
          path: rclone${{ matrix.ext }}
          if-no-files-found: error

<<<<<<< HEAD
=======
      - name: Install Go
        id: setup-go
        uses: actions/setup-go@v6
        with:
<<<<<<< HEAD
          go-version: '~1.26.3'
=======
          go-version: '~1.26.5'
>>>>>>> v1.74.4
          check-latest: true
          cache: false
          
      - name: Module cache
        uses: actions/cache@v5
        with:
          path: ~/go/pkg/mod
          key: go-mod-${{ steps.get-runner-parameters.outputs.year-week }}-${{ hashFiles('go.sum') }}
          restore-keys: |
            go-mod-${{ steps.get-runner-parameters.outputs.year-week }}-
            go-mod-

      - name: Cache
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/go-build
            ~/.cache/golangci-lint
          key: golangci-lint-${{ steps.get-runner-parameters.outputs.runner-os-version }}-go${{ steps.setup-go.outputs.go-version }}-${{ steps.get-runner-parameters.outputs.year-week }}-${{ hashFiles('go.sum') }}
          restore-keys: golangci-lint-${{ steps.get-runner-parameters.outputs.runner-os-version }}-go${{ steps.setup-go.outputs.go-version }}-${{ steps.get-runner-parameters.outputs.year-week }}-

      - name: Code quality test (Linux)
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          skip-cache: true

      - name: Code quality test (Windows)
        uses: golangci/golangci-lint-action@v9
        env:
          GOOS: "windows"
        with:
          version: latest
          skip-cache: true

      - name: Code quality test (macOS)
        uses: golangci/golangci-lint-action@v9
        env:
          GOOS: "darwin"
        with:
          version: latest
          skip-cache: true

      - name: Code quality test (FreeBSD)
        uses: golangci/golangci-lint-action@v9
        env:
          GOOS: "freebsd"
        with:
          version: latest
          skip-cache: true

      - name: Code quality test (OpenBSD)
        uses: golangci/golangci-lint-action@v9
        env:
          GOOS: "openbsd"
        with:
          version: latest
          skip-cache: true

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Scan for vulnerabilities
        run: govulncheck ./...

      - name: Check Markdown format
        uses: DavidAnson/markdownlint-cli2-action@v20
        with:
          globs: |
            CONTRIBUTING.md
            MAINTAINERS.md
            README.md
            RELEASE.md
            CODE_OF_CONDUCT.md
            librclone\README.md
            backend\s3\README.md
            docs/content/{_index,authors,bugs,changelog,docs,downloads,faq,filtering,gui,install,licence,overview,privacy}.md

      - name: Scan edits of autogenerated files
        run: bin/check_autogenerated_edits.py 'origin/${{ github.base_ref }}'
        if: github.event_name == 'pull_request'
>>>>>>> 7dcbc8b4f3d89818642a22977d4307f836eafb6c

<<<<<<< HEAD
  # --- JOB: ANDROID ---
=======
      - name: Trim module cache before saving
        if: always()
        run: |
          # The downloaded module .zip archives are not needed to build from
          # the already-extracted module cache, so drop them before the cache
          # is saved - this roughly halves the module cache size.
          modcache=$(go env GOMODCACHE | tr '\\' '/')
          find "$modcache/cache/download" -name '*.zip' -type f -delete 2>/dev/null || true

>>>>>>> v1.74.4
  android:
    name: Build Android
    runs-on: ubuntu-latest
    timeout-minutes: 30

    # FIX: aggiunto 'github.event_name == 'push'' come nel job build
    if: |
      github.event_name == 'push' ||
      inputs.target == 'all'      ||
      inputs.target == 'android'

    steps:
      - name: Get runner parameters
        id: get-runner-parameters
        run: |
          echo "year-week=$(date -u "+%Y%V")" >> $GITHUB_OUTPUT
          echo "runner-os-version=$ImageOS" >> $GITHUB_OUTPUT

      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Go
        id: setup-go
        uses: actions/setup-go@v6
        with:
<<<<<<< HEAD
<<<<<<< HEAD
          go-version: '1.26.x'
          check-latest: true
          cache: true
=======
          go-version: '~1.26.3'
>>>>>>> 7dcbc8b4f3d89818642a22977d4307f836eafb6c
=======
          go-version: '~1.26.5'
          # Caching is handled explicitly below to share the module cache
          # with the other jobs - see the build job for the rationale.
          cache: false

      - name: Get Go paths
        id: go-paths
        run: |
          echo "build-cache=$(go env GOCACHE)" >> $GITHUB_OUTPUT
          echo "mod-cache=$(go env GOMODCACHE)" >> $GITHUB_OUTPUT

      - name: Module cache
        uses: actions/cache@v5
        with:
          path: ${{ steps.go-paths.outputs.mod-cache }}
          key: go-mod-${{ steps.get-runner-parameters.outputs.year-week }}-${{ hashFiles('go.sum') }}
          restore-keys: |
            go-mod-${{ steps.get-runner-parameters.outputs.year-week }}-
            go-mod-

      - name: Build cache
        uses: actions/cache@v5
        with:
          path: ${{ steps.go-paths.outputs.build-cache }}
          key: go-build-android-${{ steps.get-runner-parameters.outputs.runner-os-version }}-go${{ steps.setup-go.outputs.go-version }}-${{ steps.get-runner-parameters.outputs.year-week }}-${{ hashFiles('go.sum') }}
          restore-keys: |
            go-build-android-${{ steps.get-runner-parameters.outputs.runner-os-version }}-go${{ steps.setup-go.outputs.go-version }}-${{ steps.get-runner-parameters.outputs.year-week }}-
            go-build-android-${{ steps.get-runner-parameters.outputs.runner-os-version }}-go${{ steps.setup-go.outputs.go-version }}-
>>>>>>> v1.74.4

      - name: Build Android Targets
        run: |
          set -euo pipefail
          VERSION="${{ github.ref_name }}"
          if [ -z "$VERSION" ]; then VERSION="v1.73.2-android-extra"; fi
          FLAGS="-s -w -buildid= -X github.com/rclone/rclone/fs.Version=${VERSION}"

          mkdir -p build build-logs

          NDK_ROOT="$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin"
          API="23"

          for ARCH in arm arm64 386 amd64; do
            echo "::group::Building for android/$ARCH"

            export GOOS=android
            export GOARCH=$ARCH
            export CGO_ENABLED=1

            if [ "$ARCH" == "arm" ]; then
              CC_BIN="$NDK_ROOT/armv7a-linux-androideabi${API}-clang"
            elif [ "$ARCH" == "arm64" ]; then
              CC_BIN="$NDK_ROOT/aarch64-linux-android${API}-clang"
            elif [ "$ARCH" == "386" ]; then
              CC_BIN="$NDK_ROOT/i686-linux-android${API}-clang"
            elif [ "$ARCH" == "amd64" ]; then
              CC_BIN="$NDK_ROOT/x86_64-linux-android${API}-clang"
            fi

            export CC="$CC_BIN"
            echo "Using compiler: $CC"

            go build -v -trimpath -mod=readonly -ldflags "$FLAGS" \
              -o build/rclone-android-$ARCH . 2>&1 | tee "build-logs/build-android-$ARCH.log"

            if [ -f "build/rclone-android-$ARCH" ]; then
              echo "✅ Successfully built rclone-android-$ARCH"
              ls -lh build/rclone-android-$ARCH
            else
              echo "❌ Failed to build rclone-android-$ARCH"
              exit 1
            fi
            echo "::endgroup::"
          done

      # FIX: carica anche i binari Android sulla release (solo su tag push)
      - name: Upload Android zip to GitHub Release (tag push only)
        if: startsWith(github.ref, 'refs/tags/')
        env:
<<<<<<< HEAD
          GH_TOKEN: ${{ github.token }}
        run: |
          zip rclone-android-all.zip -j build/rclone-android-*
          gh release upload ${{ github.ref_name }} rclone-android-all.zip --clobber

      - name: Upload Android Artifacts (per uso interno)
        uses: actions/upload-artifact@v7
        with:
          name: rclone-android-all
          path: build/rclone-android-*
          if-no-files-found: error

      - name: Upload Android build logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: logs-android
          path: build-logs/build-android-*.log


  # --- JOB: PUBLISH (solo su tag push, dopo che build e android sono completati) ---
  publish:
    name: Publish to WinGet
    runs-on: ubuntu-latest
    needs: [build, android]  # aspetta che tutti i build siano finiti
    if: startsWith(github.ref, 'refs/tags/')  # solo su tag push, mai su workflow_dispatch

    steps:
      # Nota: winget-releaser legge gli asset già caricati sulla release da gh release upload
      # La regex '-windows-\w+\.zip$' troverà il file 'rclone-windows-amd64.zip'
      - name: Submit to WinGet via winget-releaser
        uses: vedantmgoyal2009/winget-releaser@v2
        with:
          identifier: Rclone.Rclone
          installers-regex: '-windows-\w+\.zip$'
          max-versions-to-keep: 0
          release-repository: rclone-extra
          release-tag: ${{ github.ref_name }}
          fork-user: gulp79
          token: ${{ secrets.WINGET_TOKEN }}  # PAT con accesso in scrittura al fork winget-pkgs
=======
          RCLONE_CONFIG_PASS: ${{ secrets.RCLONE_CONFIG_PASS }}
        # Upload artifacts if not a PR && not a fork
        if: env.RCLONE_CONFIG_PASS != '' && github.head_ref == '' && github.repository == 'rclone/rclone'

      - name: Trim module cache before saving
        if: always()
        run: |
          # The downloaded module .zip archives are not needed to build from
          # the already-extracted module cache, so drop them before the cache
          # is saved - this roughly halves the module cache size.
          modcache=$(go env GOMODCACHE | tr '\\' '/')
          find "$modcache/cache/download" -name '*.zip' -type f -delete 2>/dev/null || true
>>>>>>> v1.74.4
